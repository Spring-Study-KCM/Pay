spring:
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        # show formatted sql
        format_sql: true
        show_sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: 'jdbc:mysql://localhost:3307/pay_service'
    username: root
    password: yijuchoi711
